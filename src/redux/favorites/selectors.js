export const selectFavoriteTeachers = (state) => state.favorites.favoriteTeachers;
export const selectIsLoading = (state) => state.favorites.isLoading;
export const selectIsError = (state) => state.favorites.isError;